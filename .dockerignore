# Dependencies (reinstalled during build)
web/node_modules
web/*/node_modules

# Build outputs (rebuilt during Docker build)
web/api-server/dist
web/tg-web/dist

# Replit-specific files
.replit
replit.nix
.cache
.local
.agents
zipFile.zip
*.zip

# Dev files
.git
.gitignore
start.sh

# Logs & runtime state
*.log
~/.tg-monitor*
